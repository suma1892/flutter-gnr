import 'package:flutter/material.dart';

class TypeOfCars extends StatelessWidget {
  const TypeOfCars({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('TypeOfCars');
  }
}
