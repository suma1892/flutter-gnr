import 'package:flutter/material.dart';

class RentalCompleteness extends StatelessWidget {
  const RentalCompleteness({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Rental Completeness');
  }
}